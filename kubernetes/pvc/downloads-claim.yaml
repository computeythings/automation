apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-claim
spec:
  storageClassName: ""
  volumeName: nfs-downloads
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1G

