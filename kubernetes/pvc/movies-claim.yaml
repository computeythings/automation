apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movies-claim
spec:
  storageClassName: ""
  volumeName: nfs-movies
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1G

